{
  "name": "web",
  "version": "1.0.0",
  "description": "Egg web console",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "nantipov",
  "license": "ISC",
  "devDependencies": {
    "gts": "^3.1.1",
    "typescript": "^4.8.3",
    "@types/node": "^14.11.2"
  }
}
